[package]
name = "shadowforge"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["process"] }
rand = "0.8"

[lib]
name = "shadowforge"
path = "src/lib.rs"
