[package]
name = "shadowforge"
version = "0.1.0"
edition = "2021"
description = "Kernel-level resilient infrastructure for high-adversity environments"
license = "GPL-3.0"

[dependencies]
rand = "0.8"
